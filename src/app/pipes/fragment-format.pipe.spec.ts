import { FragmentFormatPipe } from './fragment-format.pipe';

describe('FragmentFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FragmentFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
